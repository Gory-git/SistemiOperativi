package sisop.threads.esercizi5;

import sisop.threads.esercizi5.produttoreConsumatore.Buffer;
import sisop.threads.esercizi5.produttoreConsumatore.BufferSem;
import sisop.threads.esercizi5.produttoreConsumatore.Consumatore;
import sisop.threads.esercizi5.produttoreConsumatore.Produttore;

public class Test
{
    public static void main(String[] args)
    {   /*
        ESERCIZIO PRODUTTORI-CONSUMATORI

        int dimensione = 10;
        int numProduttori = 10;
        int numConsumatori = 10;

        Buffer buffer = new BufferSem(dimensione);

        for (int i = 0; i < numProduttori; i++)
        {
            new Produttore(buffer).start();
        }
        for (int i = 0; i < numConsumatori; i++)
        {
            new Consumatore(buffer).start();
        }
        */
    }
}
